# Experimental environment provenance

The project lead confirmed that the environment recorded in this audit is the same environment used for the original TF-IDF and BERTimbau training runs.

Confirmed: True

Statement:

The environment recorded by this notebook is the same environment used for the original TF-IDF and BERTimbau training runs.

The files `01_training_and_audit_environment.csv` and `requirements_snapshot.txt` provide the runtime snapshot used for reproducibility reporting.
